# Logs directory
LOG_DIR=${HOME}/cardano/logs
# Topology configuration output directory
CONFIG_DIR=${HOME}/cardano/config
# Custom peers for the topology updater
CUSTOM_PEERS="<ip>,<port>|<ip>,<port>|<ip>,<port>"